syntax = "proto3";

package wavelet;

message Delta {
    bytes account = 1;
    string key = 2;

    bytes old_value = 3;
    bytes new_value = 4;
}

message Deltas {
    message List {
        repeated Delta list = 1;
    }

    map<string, List> deltas = 1;
}